---
phase: 01-baseline-comparison-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - comparison-core.js
  - app.js
  - tests/comparison-core.test.mjs
files-modified:
  - comparison-core.js
  - app.js
  - tests/comparison-core.test.mjs
autonomous: true
requirements: [WAL-01]
user_setup: []
objective: Extract deterministic comparison logic with regression safety checks
must_haves:
    truths:
      - "User sees wallet cards ranked highest-to-lowest multiplier for selected category."
      - "Ranking order is deterministic for the same wallet and category."
      - "Cards without selected category and without other fallback are excluded from results."
    artifacts:
      - path: comparison-core.js
        provides: Deterministic comparison + fallback logic
        min_lines: 40
      - path: tests/comparison-core.test.mjs
        provides: Regression coverage for ranking rules
        min_lines: 40
    key_links:
      - from: app.js
        to: comparison-core.js
        via: renderComparison delegates ranking to shared core
        pattern: "computeComparisonResults"
      - from: tests/comparison-core.test.mjs
        to: comparison-core.js
        via: node test imports and validates behavior
        pattern: "comparison-core"
---

<objective>
Extract and stabilize comparison/ranking logic with regression-focused checks.

Purpose: Lock down WAL-01 behavior before catalog features increase complexity.
Output: Shared comparison core and automated regression tests that enforce ranking determinism.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-comparison-hardening/01-CONTEXT.md
@app.js
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Create shared comparison core module</name>
  <files>comparison-core.js</files>
  <action>Implement pure comparison helpers with no DOM dependencies: category score resolution (match -> other -> exclude), deterministic sorting (multiplier desc, card name asc), and multiplier display formatting that trims trailing zeroes. Export functions for browser use and Node test use to avoid duplicated logic between runtime and tests.</action>
  <verify>node --check comparison-core.js</verify>
  <done>`comparison-core.js` exports deterministic comparison helpers and returns source metadata needed by UI rendering.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Wire app comparison rendering to shared core</name>
  <files>app.js</files>
  <action>Refactor `renderComparison` to delegate scoring and ordering to `comparison-core.js` helpers. Preserve current form/card CRUD flow, but update ranking data path so selected-category results are driven by shared core outputs rather than inline ad-hoc sorting.</action>
  <verify>node --check app.js</verify>
  <done>`renderComparison` consumes shared ranking outputs and renders ranked cards based on deterministic ordering rules.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add regression tests for comparison behavior</name>
  <files>tests/comparison-core.test.mjs</files>
  <action>Create Node built-in tests covering tie-break alphabetical ordering, `other` fallback behavior, exclusion when no qualifying reward exists, and multiplier display formatting. Keep test fixtures simple and focused on edge cases from phase context decisions.</action>
  <verify>node --test tests/comparison-core.test.mjs</verify>
  <done>Automated tests fail on regression in ranking/fallback rules and pass on expected behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/comparison-core.test.mjs` passes
- [ ] `node --check comparison-core.js app.js` passes
- [ ] Ranking for same fixture input is deterministic across repeated runs
</verification>

<success_criteria>
- All tasks completed
- Deterministic WAL-01 comparison logic is centralized in one shared module
- Regression tests cover tie-break, fallback, exclusion, and display formatting
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-comparison-hardening/01-01-SUMMARY.md`
</output>
