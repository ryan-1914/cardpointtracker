---
phase: 01-baseline-comparison-hardening
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - app.js
  - styles.css
  - index.html
files-modified:
  - app.js
  - styles.css
  - index.html
autonomous: true
requirements: [WAL-02]
user_setup: []
objective: Harden top-card callout and edge-state messaging clarity
must_haves:
    truths:
      - "User can clearly identify the top-ranked card for the selected category."
      - "Top-card callout updates immediately when category changes."
      - "When no qualifying cards exist, UI shows explicit no-qualifiers guidance."
    artifacts:
      - path: app.js
        provides: Top-card callout and edge-state rendering behavior
        min_lines: 250
      - path: styles.css
        provides: Medium inline callout and guidance-state styling
        min_lines: 200
    key_links:
      - from: app.js
        to: index.html
        via: result and ranking nodes are updated each category change
        pattern: "els\\.result|els\\.ranking"
      - from: app.js
        to: styles.css
        via: class-based styling for callout and muted guidance states
        pattern: "classList\\."
---

<objective>
Align main screen presentation for consistent top-card visibility and messaging.

Purpose: Deliver WAL-02 clarity so users can quickly trust and act on the top recommendation.
Output: Refined callout, ranking row labels, and edge-state messaging on the comparison screen.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-comparison-hardening/01-CONTEXT.md
@.planning/phases/01-baseline-comparison-hardening/01-01-PLAN.md
@app.js
@index.html
@styles.css
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Harden top-card callout behavior and copy</name>
  <files>app.js</files>
  <action>Implement medium inline top-card callout output showing card name + multiplier only. Ensure callout updates on every category change, uses explicit no-qualifying-cards message when filtered comparison set is empty, and keeps source labels (`category match`/`other fallback`) visible in ranking rows for clarity.</action>
  <verify>node --check app.js</verify>
  <done>Top-card callout is clear, immediate, and consistent with phase context decisions for all category selections.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Apply edge-state presentation and guidance styling</name>
  <files>styles.css, index.html</files>
  <action>Adjust compare panel markup/classes (if needed) and styles to present callout as medium inline highlight and edge-state guidance as readable muted hints. Preserve responsive behavior and existing visual language while improving clarity for empty wallet, single-card, and no-qualifier states.</action>
  <verify>node --check app.js && rg -n \"result|ranking|muted|best\" styles.css index.html</verify>
  <done>Comparison area consistently communicates top recommendation and edge states without requiring user guesswork.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check app.js` passes
- [ ] `node --test tests/comparison-core.test.mjs` still passes (no regression from Plan 01)
- [ ] Category switch updates callout/ranking text immediately and handles no-qualifier state explicitly
</verification>

<success_criteria>
- All tasks completed
- WAL-02 callout clarity implemented on main comparison screen
- Edge states follow context decisions for messaging and guidance
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-comparison-hardening/01-02-SUMMARY.md`
</output>
