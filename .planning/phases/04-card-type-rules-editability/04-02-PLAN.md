---
phase: 04-card-type-rules-editability
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app.js
  - wallet-core.js
  - styles.css
  - tests/wallet-core.test.mjs
files-modified:
  - app.js
  - wallet-core.js
  - styles.css
  - tests/wallet-core.test.mjs
autonomous: true
requirements: [CAT-06, CUS-02]
user_setup: []
objective: Gate edit affordances and runtime edit paths to custom cards only while preserving custom edit behavior
must_haves:
    truths:
      - "Catalog-origin wallet cards cannot enter editable state from the wallet UI."
      - "Custom-origin wallet cards remain editable through the existing form workflow."
      - "Runtime guards prevent catalog edits even if stale or crafted edit events fire."
    artifacts:
      - path: app.js
        provides: Origin-aware UI gating and runtime edit guards for wallet entries
        min_lines: 560
      - path: wallet-core.js
        provides: Editability helper APIs keyed by wallet origin type
        min_lines: 220
      - path: styles.css
        provides: Visual treatment for non-editable catalog wallet cards and edit-availability cues
        min_lines: 360
      - path: tests/wallet-core.test.mjs
        provides: Regression coverage for editability helper semantics by origin type
        min_lines: 220
    key_links:
      - from: app.js
        to: wallet-core.js
        via: Edit button rendering and edit handler guards use shared origin-aware helper predicates
        pattern: "canEdit|isCatalog|originType"
      - from: app.js
        to: styles.css
        via: Catalog wallet entries render non-editable visual state classes
        pattern: "card-source|is-readonly|catalog"
      - from: tests/wallet-core.test.mjs
        to: wallet-core.js
        via: Tests lock custom-edit allowed and catalog-edit blocked behavior
        pattern: "canEdit"
---

<objective>
Deliver editability rules by card origin: catalog immutable, custom editable.

Purpose: Satisfy CAT-06 and CUS-02 without regressing current wallet edit UX for custom cards.
Output: Catalog entries cannot be edited through UI or runtime path; custom entries continue to edit successfully.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-card-type-rules-editability/04-01-PLAN.md
@app.js
@wallet-core.js
@styles.css
@index.html
@tests/wallet-core.test.mjs
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add origin-aware editability helpers and enforce runtime edit guards</name>
  <files>wallet-core.js, app.js, tests/wallet-core.test.mjs</files>
  <action>Add shared helper predicates for wallet editability and use them in app edit handlers so catalog-origin cards cannot enter or persist through edit operations. Include stale-event/runtime safety checks to block catalog edits even when UI state is outdated while preserving custom-card edit updates.</action>
  <verify>node --check wallet-core.js app.js && node --test tests/wallet-core.test.mjs</verify>
  <done>Runtime edit path rejects catalog entries and still allows custom entries, with tests proving helper semantics.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Gate wallet edit controls to custom cards and expose clear readonly cues</name>
  <files>app.js, styles.css, index.html</files>
  <action>Update wallet row rendering to hide or disable edit affordances for catalog cards while leaving custom card edit controls unchanged. Add concise readonly visual cues so users can distinguish immutable catalog entries from editable custom entries at a glance, aligned with existing UI style direction.</action>
  <verify>node --check app.js && rg -n "data-edit-id|catalog|readonly|edit" app.js styles.css index.html</verify>
  <done>Catalog rows are visibly non-editable and custom rows keep functional edit controls.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check app.js wallet-core.js` passes
- [ ] `node --test tests/wallet-core.test.mjs` passes
- [ ] Catalog cards cannot be edited from wallet UI
- [ ] Custom cards still follow normal edit workflow
- [ ] Runtime edit guard blocks catalog edit attempts beyond UI gating
</verification>

<success_criteria>
- All tasks completed
- CAT-06 and CUS-02 behavior is enforced by shared helper and runtime/UI gates
- Catalog immutability is visible and consistent in wallet UI
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-card-type-rules-editability/04-02-SUMMARY.md`
</output>
