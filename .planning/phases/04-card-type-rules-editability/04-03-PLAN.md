---
phase: 04-card-type-rules-editability
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - app.js
  - wallet-core.js
  - tests/wallet-core.test.mjs
  - tests/comparison-core.test.mjs
  - index.html
files-modified:
  - app.js
  - wallet-core.js
  - tests/wallet-core.test.mjs
  - tests/comparison-core.test.mjs
  - index.html
autonomous: true
requirements: [CUS-03]
user_setup: []
objective: Preserve and verify custom create/edit/delete workflows while catalog protections remain intact
must_haves:
    truths:
      - "Users can create, edit, and delete custom wallet cards after origin-rule hardening."
      - "Deleting a custom card does not alter catalog definitions or catalog card availability logic."
      - "Comparison ranking remains correct for mixed custom and catalog wallets after custom CRUD actions."
    artifacts:
      - path: app.js
        provides: Stable custom CRUD runtime flow with origin-aware guards and synchronized rerender behavior
        min_lines: 560
      - path: wallet-core.js
        provides: Helper support for custom delete/edit predicates and mixed-wallet normalization consistency
        min_lines: 220
      - path: tests/wallet-core.test.mjs
        provides: Regression tests for custom create/edit/delete invariants alongside catalog protections
        min_lines: 220
      - path: tests/comparison-core.test.mjs
        provides: Guardrail tests showing ranking remains deterministic with mixed wallet sources
        min_lines: 90
    key_links:
      - from: app.js
        to: wallet-core.js
        via: Custom CRUD mutations use shared helper predicates without weakening catalog protections
        pattern: "canDelete|canEdit|originType"
      - from: app.js
        to: tests/comparison-core.test.mjs
        via: Mixed wallet mutation behavior remains compatible with ranking core expectations
        pattern: "computeComparisonResults"
      - from: tests/wallet-core.test.mjs
        to: wallet-core.js
        via: Tests validate custom CRUD remains functional while catalog restrictions stay enforced
        pattern: "custom|delete|edit"
---

<objective>
Validate and preserve custom-card management UX under new origin-type rules.

Purpose: Complete CUS-03 and ensure the full custom CRUD loop remains healthy after catalog immutability gates.
Output: Custom create/edit/delete continues to work, catalog protections hold, and ranking behavior stays deterministic.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-card-type-rules-editability/04-01-PLAN.md
@.planning/phases/04-card-type-rules-editability/04-02-PLAN.md
@app.js
@wallet-core.js
@index.html
@tests/wallet-core.test.mjs
@tests/comparison-core.test.mjs
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Lock custom delete/create/edit parity with helper-backed runtime checks</name>
  <files>app.js, wallet-core.js, tests/wallet-core.test.mjs</files>
  <action>Refine custom-card mutation flows so create, edit, and delete paths remain fully functional after introducing origin-based gating, while catalog cards stay protected. Add regression tests that cover custom CRUD loops and explicitly prove catalog definitions/addability logic is unaffected by custom-card deletion.</action>
  <verify>node --check app.js wallet-core.js && node --test tests/wallet-core.test.mjs</verify>
  <done>Custom CRUD loop is preserved with tests, and catalog protections are unaffected by custom-card mutations.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Validate mixed-wallet ranking behavior after custom CRUD mutations</name>
  <files>tests/comparison-core.test.mjs, app.js, index.html</files>
  <action>Add targeted mixed-wallet regression checks and minor runtime/view adjustments (if needed) so ranking output remains deterministic and accurate after custom create/edit/delete operations alongside catalog cards. Keep scope limited to preserving behavior and clarity, not redesigning ranking logic.</action>
  <verify>node --check app.js && node --test tests/comparison-core.test.mjs tests/wallet-core.test.mjs</verify>
  <done>Mixed wallet ranking remains stable after custom CRUD actions and supporting checks pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check app.js wallet-core.js` passes
- [ ] `node --test tests/wallet-core.test.mjs tests/comparison-core.test.mjs` passes
- [ ] Custom cards can still be created, edited, and deleted
- [ ] Deleting custom cards does not impact catalog card definitions/addability
- [ ] Ranking remains deterministic for mixed custom+catalog wallet states
</verification>

<success_criteria>
- All tasks completed
- CUS-03 is verified with regression protection
- Custom-card management UX remains intact under origin-based rules
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-card-type-rules-editability/04-03-SUMMARY.md`
</output>
