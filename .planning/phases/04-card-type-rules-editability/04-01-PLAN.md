---
phase: 04-card-type-rules-editability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wallet-core.js
  - app.js
  - tests/wallet-core.test.mjs
files-modified:
  - wallet-core.js
  - app.js
  - tests/wallet-core.test.mjs
autonomous: true
requirements: [CUS-01]
user_setup: []
objective: Introduce explicit origin/type model invariants across wallet entities while preserving custom card creation
must_haves:
    truths:
      - "Newly created custom cards persist with explicit custom origin metadata."
      - "Legacy wallet cards without origin metadata are normalized into safe custom entities on load."
      - "Wallet helper APIs expose deterministic type checks for downstream edit/delete rules."
    artifacts:
      - path: wallet-core.js
        provides: Explicit origin/type helper APIs and normalization rules for custom/catalog entities
        min_lines: 220
      - path: tests/wallet-core.test.mjs
        provides: Regression tests for custom-origin normalization, legacy migration, and helper behavior
        min_lines: 220
      - path: app.js
        provides: Runtime integration of wallet normalization and persistence-safe custom card creation path
        min_lines: 560
    key_links:
      - from: app.js
        to: wallet-core.js
        via: App initialization and save flow route all wallet entities through origin-aware normalization helpers
        pattern: "normalizeWalletCard|normalizeWalletCards|ORIGIN_CUSTOM"
      - from: tests/wallet-core.test.mjs
        to: wallet-core.js
        via: Tests assert explicit custom-origin shape and legacy-card normalization semantics
        pattern: "custom|origin|normalize"
---

<objective>
Establish explicit wallet origin/type contracts that keep custom-card create flow robust.

Purpose: Fulfill CUS-01 foundations and remove ambiguity before editability gating.
Output: Wallet entities carry reliable origin metadata and app runtime preserves/normalizes that shape end-to-end.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-catalog-to-wallet-integration/03-01-SUMMARY.md
@.planning/phases/03-catalog-to-wallet-integration/03-02-SUMMARY.md
@.planning/phases/03-catalog-to-wallet-integration/03-03-SUMMARY.md
@app.js
@wallet-core.js
@tests/wallet-core.test.mjs
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Harden wallet-core origin contracts and legacy normalization</name>
  <files>wallet-core.js, tests/wallet-core.test.mjs</files>
  <action>Extend wallet-core so every normalized wallet entity has explicit origin metadata and helper-backed type guards for custom versus catalog cards. Add deterministic migration behavior for persisted legacy cards missing origin fields and add focused tests that prove custom-origin defaults, catalog-origin retention, and helper accuracy for downstream gating rules.</action>
  <verify>node --check wallet-core.js && node --test tests/wallet-core.test.mjs</verify>
  <done>Wallet-core guarantees explicit origin/type shape for all wallet entities and tests lock the behavior for both legacy and new records.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Integrate origin-aware normalization into custom create/load runtime paths</name>
  <files>app.js, wallet-core.js, tests/wallet-core.test.mjs</files>
  <action>Wire the app runtime to normalize cards through wallet-core on read and custom-card save paths so manual card creation always persists with custom origin metadata and migrated legacy cards are safe for future editability gating. Keep the current create flow UX intact while ensuring normalized entities remain comparison-compatible and persistence-safe.</action>
  <verify>node --check app.js wallet-core.js && node --test tests/wallet-core.test.mjs tests/comparison-core.test.mjs</verify>
  <done>Custom card creation and app bootstrap both produce origin-explicit wallet entities without breaking ranking behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check app.js wallet-core.js` passes
- [ ] `node --test tests/wallet-core.test.mjs tests/comparison-core.test.mjs` passes
- [ ] New custom cards persist with explicit custom origin metadata
- [ ] Legacy cards missing origin metadata normalize to safe custom entities
</verification>

<success_criteria>
- All tasks completed
- CUS-01 foundation behavior is explicit in wallet entity model
- Runtime create/load paths use origin-aware normalization end-to-end
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-card-type-rules-editability/04-01-SUMMARY.md`
</output>
