---
phase: 06-improve-mobile-experience
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app.js
  - styles.css
  - index.html
files-modified:
  - app.js
  - styles.css
  - index.html
autonomous: true
requirements: [WAL-03, CAT-01, CAT-02, CAT-03, CAT-04, CUS-02]
user_setup: []
objective: Improve mobile interaction clarity so catalog expansion, comparison scanning, and edit transitions are immediately understandable on touch devices
must_haves:
  truths:
    - "Key touch interactions visibly succeed on mobile without requiring blind scrolling or extra discovery work."
    - "Users can reach and understand comparison results, catalog controls, and edit state quickly after each major action."
    - "Mobile interaction polish preserves current catalog search/filter/add behavior and mixed-wallet comparison semantics."
  artifacts:
    - path: app.js
      provides: Viewport-aware interaction behavior for mobile reveal, focus, and comparison visibility improvements
      min_lines: 847
    - path: styles.css
      provides: Visual affordances that make mobile interaction states clear without adding noisy chrome
      min_lines: 457
    - path: index.html
      provides: Any minimal structural affordances required for mobile-specific helper text or sticky controls
      min_lines: 125
  key_links:
    - from: app.js
      to: index.html
      via: Toggle, ranking, and edit interactions remain wired through the existing compare/catalog/form DOM ids
      pattern: "catalogToggle|catalogPanel|ranking|cardForm"
    - from: app.js
      to: styles.css
      via: Mobile interaction states should use explicit classes/affordances instead of implicit visual changes
      pattern: "expanded|collapsed|editing|ranking"
    - from: index.html
      to: app.js
      via: Any helper affordances for mobile comparison or catalog discovery must stay compatible with existing event delegation
      pattern: "data-|aria-|button|section"
---

<objective>
Make mobile interactions feel explicit, not accidental.

Purpose: Phase 05 closed one concrete mobile gap for the catalog, but the broader app still depends on users noticing state changes across a long single-column page. This plan sharpens the visible feedback around comparison, catalog, and edit interactions on touch devices.
Output: Mobile users can tell what changed after they open the catalog, expand rankings, or enter edit mode, without needing trial-and-error scrolling.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mixed-wallet-ux-validation/05-VERIFICATION.md
@.planning/phases/05-mixed-wallet-ux-validation/05-03-SUMMARY.md
@.planning/phases/06-improve-mobile-experience/06-01-PLAN.md
@index.html
@styles.css
@app.js
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Improve mobile compare and catalog state visibility</name>
  <files>app.js, styles.css, index.html</files>
  <action>Review the existing compare and catalog interactions on phone-sized viewports and add the smallest set of UX cues needed to make state changes obvious. Candidate improvements include better in-viewport reveal targets, clearer overflow affordances for ranking, or lightweight helper copy/state treatment when compare and catalog content moves below the fold. Keep the current catalog add/search/filter logic intact.</action>
  <verify>
    <automated>node --check app.js && rg -n "catalogToggle|catalogPanel|ranking-expand|scrollIntoView|aria-expanded|result" app.js styles.css index.html</automated>
    <manual>Open the app at a mobile viewport and confirm it is visually obvious when the catalog opens and when additional ranked cards are revealed.</manual>
  </verify>
  <done>Mobile compare and catalog interactions give immediate feedback, so users can tell what changed after tapping without hunting around the page.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Keep edit-mode transitions discoverable on long mobile pages</name>
  <files>app.js, styles.css</files>
  <action>Refine the current edit-mode jump-to-form behavior so mobile users clearly understand when they are editing an existing card and how to exit that state. Preserve the current edit permissions and wallet behavior, but strengthen the viewport positioning, visual treatment, or action visibility around edit mode if the current experience is too easy to miss on a long page.</action>
  <verify>
    <automated>node --check app.js && rg -n "startEdit|scrollIntoView|formTitle|cancelEdit|editingCardId" app.js styles.css</automated>
    <manual>Select Edit on a wallet card at a mobile viewport and confirm the form change is obvious and easy to cancel.</manual>
  </verify>
  <done>Entering and leaving edit mode is obvious on mobile, with the existing custom-vs-catalog edit rules preserved.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Mobile users can tell when the catalog has opened and where the revealed controls are
- [ ] Ranking expansion remains understandable when more than four cards qualify
- [ ] Entering edit mode on mobile clearly signals that the form is now editing an existing card
- [ ] Catalog add/search/filter behavior still works exactly as before
- [ ] `node --check app.js` passes
</verification>

<success_criteria>
- All tasks completed
- Mobile interaction feedback is visible without extra explanatory clutter
- Catalog and comparison remain fast to scan on touch devices
- Edit-mode affordances are clearer without changing card-origin rules
</success_criteria>

<output>
After completion, create `.planning/phases/06-improve-mobile-experience/06-02-SUMMARY.md`
</output>
