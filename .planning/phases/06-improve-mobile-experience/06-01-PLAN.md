---
phase: 06-improve-mobile-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - styles.css
  - app.js
files-modified:
  - index.html
  - styles.css
  - app.js
autonomous: true
requirements: [WAL-03, CUS-01, CUS-02]
user_setup: []
objective: Establish a mobile-first layout baseline so compare, wallet, and add-card flows stay readable and operable on phone-sized viewports
must_haves:
  truths:
    - "Phone-width layouts avoid cramped controls, clipped text, and side-scrolling across the main compare, wallet, and add-card panels."
    - "Primary mobile actions remain easy to tap, with stacked or full-width controls where the current layout is too dense."
    - "Any mobile-specific layout adjustments preserve the current desktop information hierarchy and mixed-wallet behavior."
  artifacts:
    - path: index.html
      provides: Stable semantic structure for compare, wallet, and form sections that mobile layout hooks can target
      min_lines: 125
    - path: styles.css
      provides: Responsive spacing, stacking, and control-sizing rules tuned for phone-width screens
      min_lines: 457
    - path: app.js
      provides: Minimal state/render support needed if mobile-only helper classes or viewport-aware hooks are required
      min_lines: 847
  key_links:
    - from: index.html
      to: styles.css
      via: Existing panel, header, form, and list structure must support denser mobile spacing without semantic rewrites
      pattern: "panel|panel-header|card-form|reward-row"
    - from: app.js
      to: styles.css
      via: Any mobile-aware render hooks should map cleanly onto responsive classes rather than duplicating layout logic in JS
      pattern: "render|classList|mobile|compact"
    - from: app.js
      to: index.html
      via: Compare and edit flows must remain bound to the current DOM ids while layout shifts around them
      pattern: "categoryPicker|cardForm|cardList|ranking"
---

<objective>
Create a reliable mobile layout baseline before deeper interaction polish.

Purpose: The app already works functionally on mobile, but the current responsive surface is limited to a single reward-row collapse. This plan makes the core compare, wallet, and add-card views easier to read and operate on narrow screens without altering ranking or wallet rules.
Output: Phone-width users get cleaner spacing, stacked controls, and tap-friendly actions while desktop behavior remains intact.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mixed-wallet-ux-validation/05-VERIFICATION.md
@.planning/phases/05-mixed-wallet-ux-validation/05-03-SUMMARY.md
@index.html
@styles.css
@app.js
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Tighten panel and control layout for phone-width screens</name>
  <files>index.html, styles.css</files>
  <action>Audit the existing panel structure and add responsive layout rules for phone-width screens so headers, counts, result callouts, wallet items, and catalog cards stop feeling cramped. Prefer CSS-only improvements first: tighter but readable spacing, stacked headers where needed, and full-width control presentation when side-by-side alignment currently compresses the content.</action>
  <verify>
    <automated>rg -n "@media|panel-header|card-header|catalog-actions|form-actions|reward-row" styles.css index.html</automated>
  </verify>
  <done>The major panels and list rows read cleanly on narrow screens without clipped text, awkward wrapping, or horizontal overflow.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Make the add/edit card form feel intentional on mobile</name>
  <files>styles.css, app.js</files>
  <action>Improve the mobile ergonomics of the add/edit card form and reward rows so the user can add or update cards without wrestling with tiny controls or scattered actions. Keep the existing form flow, but use responsive button grouping, clearer field spacing, and any small viewport-aware JS hooks needed to keep edit mode obvious after the form rerenders.</action>
  <verify>
    <automated>node --check app.js && rg -n "reward-row|form-actions|scrollIntoView|cardForm|cancelEdit|saveCardBtn" app.js styles.css</automated>
  </verify>
  <done>Creating or editing a card on a phone-sized viewport keeps actions visible, fields readable, and the current edit-state affordances intact.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Phone-width screens show no obvious horizontal overflow in the main app panels
- [ ] Panel headers, counts, and action groups remain readable and tap-friendly on narrow screens
- [ ] Add/edit card workflows keep clear action placement on mobile
- [ ] `node --check app.js` passes if any JS hooks changed
- [ ] Desktop-width hierarchy still reads Compare, Your Cards, Add Card, Catalog
</verification>

<success_criteria>
- All tasks completed
- Mobile layout feels intentionally adapted rather than merely compressed
- Compare and wallet behavior remains unchanged while form ergonomics improve
- No desktop regression is introduced by the responsive rules
</success_criteria>

<output>
After completion, create `.planning/phases/06-improve-mobile-experience/06-01-SUMMARY.md`
</output>
