---
phase: 03-catalog-to-wallet-integration
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app.js
  - index.html
  - styles.css
  - wallet-core.js
  - tests/wallet-core.test.mjs
files-modified:
  - app.js
  - index.html
  - styles.css
  - wallet-core.js
  - tests/wallet-core.test.mjs
autonomous: true
requirements: [CAT-05]
user_setup: []
objective: Enforce duplicate prevention rules and user feedback for already-added cards
must_haves:
    truths:
      - "User cannot add the same catalog card to wallet more than once."
      - "Catalog rows communicate addability with explicit Add to Wallet vs Added states."
      - "Successful add interactions provide immediate inline confirmation feedback."
    artifacts:
      - path: app.js
        provides: Duplicate checks, addability state derivation, and inline catalog feedback rendering
        min_lines: 540
      - path: styles.css
        provides: Visual states for add button, added badge, and inline success feedback
        min_lines: 340
      - path: wallet-core.js
        provides: Helpers for duplicate detection keyed by catalog identity
        min_lines: 130
      - path: tests/wallet-core.test.mjs
        provides: Regression tests for duplicate-prevention semantics
        min_lines: 120
    key_links:
      - from: app.js
        to: wallet-core.js
        via: Add flow validates catalog uniqueness before persistence write
        pattern: "hasCatalogDuplicate|catalogCardAlreadyInWallet"
      - from: app.js
        to: index.html
        via: Catalog row action text and disabled state derive from wallet membership
        pattern: "Add to Wallet|Added"
      - from: app.js
        to: styles.css
        via: Inline success/duplicate feedback uses dedicated catalog feedback classes
        pattern: "catalog-feedback|catalog-add-status"
---

<objective>
Harden catalog add behavior with deterministic duplicate prevention and clear user feedback.

Purpose: Fulfill CAT-05 while honoring locked context decisions for inline add controls.
Output: Duplicate adds are blocked, rows show `Add to Wallet`/`Added`, and successful adds show brief inline feedback.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-catalog-to-wallet-integration/03-CONTEXT.md
@.planning/phases/03-catalog-to-wallet-integration/03-01-PLAN.md
@app.js
@index.html
@styles.css
@wallet-core.js
@tests/wallet-core.test.mjs
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Enforce catalog duplicate prevention in wallet write path</name>
  <files>wallet-core.js, app.js, tests/wallet-core.test.mjs</files>
  <action>Add a deterministic duplicate guard keyed by catalog card identity in shared helpers and enforce it in the add-from-catalog path before persistence writes. Cover both proactive UI-state checks and stale-state runtime guard behavior with tests so duplicate entries cannot be created through repeated or rapid interactions.</action>
  <verify>node --check wallet-core.js app.js && node --test tests/wallet-core.test.mjs</verify>
  <done>Wallet write path rejects duplicate catalog adds and regression tests prove one-instance constraint.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement locked catalog add-state UI and inline success feedback</name>
  <files>app.js, index.html, styles.css</files>
  <action>Render a row-level catalog action labeled `Add to Wallet` for addable cards, switch added rows to disabled `Added`, and show a brief inline success message near the card when add succeeds. Keep feedback local to the row per context decisions and ensure rerenders preserve correct action state from persisted wallet membership.</action>
  <verify>node --check app.js && rg -n \"Add to Wallet|Added|catalog\" app.js index.html styles.css</verify>
  <done>Catalog rows always reflect true addability state and successful adds show localized inline confirmation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check app.js wallet-core.js` passes
- [ ] `node --test tests/wallet-core.test.mjs` passes
- [ ] Same catalog card cannot be added twice
- [ ] Addable rows show `Add to Wallet`; added rows show disabled `Added`
- [ ] Successful add shows brief inline row-level confirmation
</verification>

<success_criteria>
- All tasks completed
- CAT-05 duplicate-prevention behavior is enforced and visible
- Locked Phase 3 context decisions for add interaction are implemented
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-catalog-to-wallet-integration/03-02-SUMMARY.md`
</output>
