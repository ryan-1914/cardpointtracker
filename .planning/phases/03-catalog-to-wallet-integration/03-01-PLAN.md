---
phase: 03-catalog-to-wallet-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.js
  - catalog-core.js
  - index.html
  - wallet-core.js
  - tests/wallet-core.test.mjs
files-modified:
  - app.js
  - catalog-core.js
  - index.html
  - wallet-core.js
  - tests/wallet-core.test.mjs
autonomous: true
requirements: [CAT-04]
user_setup: []
objective: Implement add-from-catalog action and wallet persistence for catalog card origin
must_haves:
    truths:
      - "User can add a catalog card into wallet state from the catalog list."
      - "Added catalog cards persist in IndexedDB and reload with wallet data."
      - "Wallet entries retain catalog origin metadata for downstream rules."
    artifacts:
      - path: wallet-core.js
        provides: Pure helpers for catalog-to-wallet conversion and wallet card normalization
        min_lines: 100
      - path: tests/wallet-core.test.mjs
        provides: Regression tests for catalog conversion and persisted shape guarantees
        min_lines: 90
      - path: app.js
        provides: Runtime wiring for add-from-catalog and persistence integration
        min_lines: 500
      - path: index.html
        provides: Catalog add-action markup hooks and wallet data attributes
        min_lines: 100
    key_links:
      - from: app.js
        to: wallet-core.js
        via: Catalog add event maps catalog rows to persisted wallet entities via shared helper
        pattern: "walletCore|createCatalogWalletCard"
      - from: app.js
        to: index.html
        via: Catalog list actions dispatch add-to-wallet events from row controls
        pattern: "catalogList|data-catalog-add-id"
      - from: tests/wallet-core.test.mjs
        to: wallet-core.js
        via: Node tests import and assert catalog wallet-shape conversion semantics
        pattern: "wallet-core"
---

<objective>
Deliver the initial catalog-to-wallet integration path and persist catalog origin metadata.

Purpose: Fulfill CAT-04 foundation behavior before duplicate and delete hardening.
Output: Catalog cards can be added into wallet state and survive reload with explicit origin data.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-catalog-to-wallet-integration/03-CONTEXT.md
@app.js
@index.html
@catalog-core.js
@tests/catalog-core.test.mjs
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add shared wallet-core helpers for catalog origin entities</name>
  <files>wallet-core.js, tests/wallet-core.test.mjs</files>
  <action>Create a browser+Node compatible wallet helper module that converts catalog cards into wallet entries with stable ids, origin metadata, timestamps, and normalized rewards shape. Add Node tests for conversion validity, deterministic ids, and persistence-safe object shape so app wiring can stay thin.</action>
  <verify>node --check wallet-core.js && node --test tests/wallet-core.test.mjs</verify>
  <done>Shared helper API exists with tests proving catalog-to-wallet entity conversion and origin metadata invariants.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Wire catalog add action into app state and IndexedDB persistence</name>
  <files>index.html, app.js, catalog-core.js</files>
  <action>Add row-level add-action hooks in catalog rendering and event delegation in app runtime that builds wallet entities from selected catalog cards, saves them to IndexedDB, updates in-memory state, and triggers wallet/comparison rerender immediately. Keep this plan focused on successful add path and persistence plumbing; duplicate prevention and rich feedback land in subsequent plans.</action>
  <verify>node --check app.js catalog-core.js && node --test tests/wallet-core.test.mjs tests/comparison-core.test.mjs</verify>
  <done>Catalog card selection writes a persisted wallet entry with catalog origin and the card appears in wallet/comparison on the same interaction cycle.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check app.js wallet-core.js catalog-core.js` passes
- [ ] `node --test tests/wallet-core.test.mjs tests/comparison-core.test.mjs` passes
- [ ] Added catalog cards persist after reload and include catalog origin metadata
</verification>

<success_criteria>
- All tasks completed
- CAT-04 add-to-wallet behavior exists with persistence
- Catalog-origin metadata is present for each catalog-sourced wallet card
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-catalog-to-wallet-integration/03-01-SUMMARY.md`
</output>
