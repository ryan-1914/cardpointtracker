---
phase: 03-catalog-to-wallet-integration
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app.js
  - styles.css
  - index.html
  - wallet-core.js
  - tests/wallet-core.test.mjs
files-modified:
  - app.js
  - styles.css
  - index.html
  - wallet-core.js
  - tests/wallet-core.test.mjs
autonomous: true
requirements: [CAT-07]
user_setup: []
objective: Support delete flow parity for catalog-sourced wallet entries
must_haves:
    truths:
      - "User can remove a previously added catalog card from wallet."
      - "Removing catalog cards updates wallet list and comparison ranking immediately."
      - "Removed catalog cards become addable again in catalog list."
    artifacts:
      - path: app.js
        provides: Catalog-card delete handling and synchronized wallet/catalog/comparison rerender flow
        min_lines: 560
      - path: wallet-core.js
        provides: Helpers for catalog-origin lookup and remove-state recomputation
        min_lines: 150
      - path: tests/wallet-core.test.mjs
        provides: Regression coverage for catalog remove behavior and re-add eligibility
        min_lines: 140
      - path: styles.css
        provides: Optional source/status presentation support for catalog wallet entries
        min_lines: 345
    key_links:
      - from: app.js
        to: wallet-core.js
        via: Delete flow re-evaluates catalog membership after removal for addability refresh
        pattern: "removeCatalog|getCatalogMembership"
      - from: app.js
        to: index.html
        via: Wallet delete actions trigger shared rerender path affecting catalog and ranking sections
        pattern: "data-delete-id|render\\("
      - from: tests/wallet-core.test.mjs
        to: wallet-core.js
        via: Tests validate remove then re-add eligibility semantics for catalog cards
        pattern: "remove"
---

<objective>
Complete catalog-to-wallet integration by delivering catalog-card delete parity.

Purpose: Fulfill CAT-07 and ensure add/remove loops remain coherent across wallet and catalog surfaces.
Output: Catalog-sourced wallet entries can be deleted cleanly and become addable again.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-catalog-to-wallet-integration/03-CONTEXT.md
@.planning/phases/03-catalog-to-wallet-integration/03-01-PLAN.md
@.planning/phases/03-catalog-to-wallet-integration/03-02-PLAN.md
@app.js
@index.html
@styles.css
@wallet-core.js
@tests/wallet-core.test.mjs
@tests/comparison-core.test.mjs
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Implement catalog delete parity in wallet data path</name>
  <files>wallet-core.js, app.js, tests/wallet-core.test.mjs</files>
  <action>Ensure catalog-sourced wallet entries participate in the same delete flow as existing cards, with helper-backed logic that updates membership state after removal. Add tests that confirm removed catalog cards are absent from wallet entities and immediately eligible for re-add from catalog controls.</action>
  <verify>node --check wallet-core.js app.js && node --test tests/wallet-core.test.mjs</verify>
  <done>Deleting a catalog wallet entry updates state deterministically and re-add eligibility is restored.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Synchronize catalog addability and comparison refresh after delete</name>
  <files>app.js, index.html, styles.css</files>
  <action>Use the existing render cycle to refresh wallet list, catalog row action states, and ranking output immediately after deleting a catalog-sourced card. Keep behavior parity with current delete UX while ensuring the corresponding catalog row transitions back from `Added` to `Add to Wallet` without full-page reload.</action>
  <verify>node --check app.js && rg -n \"data-delete-id|Add to Wallet|Added|renderComparison\" app.js index.html styles.css</verify>
  <done>Catalog-card deletion instantly updates wallet, ranking, and catalog row addability in one interaction.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check app.js wallet-core.js` passes
- [ ] `node --test tests/wallet-core.test.mjs tests/comparison-core.test.mjs` passes
- [ ] User can delete catalog-sourced wallet entries
- [ ] Removed catalog cards become addable again in catalog list
- [ ] Wallet list and ranking refresh immediately after delete
</verification>

<success_criteria>
- All tasks completed
- CAT-07 delete behavior works for catalog-sourced entries
- Add/remove catalog loop remains coherent across catalog, wallet, and comparison surfaces
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-catalog-to-wallet-integration/03-03-SUMMARY.md`
</output>
